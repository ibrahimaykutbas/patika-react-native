import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 250,
    alignSelf: 'center',
    padding: 10,
    borderRadius: 10,
  },
});
